import os

with open('app/src/main/java/com/example/cassavaleafdetector/MainActivity.kt', 'r', encoding='utf-8') as f:
    original_code = f.read()

# I will just write a new MainActivity entirely from scratch using the template, inserting my logic.
# Wait, it's safer to just provide the exact code block as text, but dealing with multiline strings in python can be tricky.
# Let's write the complete new Kotlin file directly into `app/src/main/java/com/example/cassavaleafdetector/MainActivity.kt`.
